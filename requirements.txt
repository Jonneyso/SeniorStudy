# 上海高中生学习辅助项目 - Python依赖
# 本项目使用Python标准库，无需额外安装依赖即可运行

# 如需使用虚拟环境，可通过以下命令创建：
# python -m venv venv
# venv\Scripts\activate  # Windows
# source venv/bin/activate  # macOS/Linux

# 如需安装依赖（当前项目无需额外依赖）：
# pip install -r requirements.txt
